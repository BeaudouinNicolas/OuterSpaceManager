<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="outerspacemanager.com.beaudouin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".buildings.BuildingsActivity" />
        <activity android:name=".galaxy.GalaxyActivity" />
        <activity android:name=".galaxy.GalaxyAdapter" />
        <activity android:name=".fleet.FleetActivity" />
        <activity android:name=".fleet.FleetAdapter" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".search.SearchAdapter" />
        <activity android:name=".space_shuttle.SpaceShuttleActivity" />
        <activity android:name=".space_shuttle.ShipsListAdapter" />
        <activity android:name=".space_shuttle.ShipDetailActivity"></activity>
    </application>

</manifest>